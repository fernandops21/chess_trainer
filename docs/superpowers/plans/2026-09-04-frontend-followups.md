# Frontend do ciclo A — pendências e decisões (extraído do ledger de execução, 2026-09-05)

## Decisões tomadas durante a execução
- Ruling: in-place branch instead of worktree (same reasons as before) — cost if wrong: none.
- | T8 Session.finish elapsed label | uses clock.label.replace("+","") which is the overtime, not total | Ruling: compute mm:ss from clock.elapsedMs (export mmss from useSessionClock) — cost if wrong: none |
- | T11 stray `preselected` lines | dead code the plan itself calls optional | Ruling: drop them; ?position= preselection not implemented in this cut |
- | T12 React.ChangeEvent without React import | type error under jsx: react-jsx | Ruling: `import type { ChangeEvent } from "react"` |
- Task 6: Ruling: fix round covers Important 1 (retrySubmit phase guard) and 2 (document key={puzzle.id} contract; Task 8 already mounts SessionPuzzle with key), plus minors: promotion detection over alternatives, try/catch around chess.move scoring wrong, stronger "alternativa aceita" assertion, cancelPromotion test — cost if wrong: none.
- Task 8: Ruling: fix round covers Critical 1 (LineViewer reset on clock tick: stable memo key), Important 2 (key on SingleTrain), minors 3 (StrictMode double session guard), 4 (disable Próximo while advancing), 5 (refetch failure falls back to finish with retry), 6 (clock on result panel), 7 (category tag), 8 (radio name + minutes clamp) — cost if wrong: none.
- Task 9: fix round 2/5 (1 addressed; commit 9df4449). Ruling: no third scoped re-review — the round-2 change restores the exact `<Link to="/config">` the first review had already approved — cost if wrong: none.
- Ruling: engine searches use depth-only Limit, so python-chess applies no timeout and complex positions at depth 20/multipv 3 take minutes (profile: one mistake 170 s; server job >1.5 h on a 139-ply game). Fix now, on this branch (backend-only commit): Limit(depth, time=cap) with caps analysis 15 s / puzzle 20 s / reply 10 s (configurable in PuzzleConfig/settings), TimeoutError → engine restart + EngineError so the pipeline skips the game — cost if wrong: a few puzzles found at lower effective depth on very complex positions.
- Task 10: review Approved with 2 Importants (unguarded chess.move in buildPlies positions path; mobile .gamerow grid wraps 4th column) + minors (clamp ?ply; Link>button). Ruling: fix round 1 covers both Importants + ?ply clamp + Link>button → button+navigate; queued after the backend time-cap commit to avoid concurrent commits.
- Backend time-cap commit 88c4110: review With fixes. Ruling: fix round (after Task 11 lands): close() also catches TimeoutError and falls back to killing the transport; spec sentence corrected (avoid mode has no reply search) and documents the effective +10 s python-chess baseline; skip the double restart by not restarting in analyse() when callers already do? No — keep analyse()'s restart (self-healing for callers that don't) and leave the double restart as a deferred minor — cost if wrong: ~1 s extra spawn on timeouts.
- Task 11: review Approved; Important (plan-inherited): 200 chessground instances per page. Ruling: fix round — limit 50 with "Mostrar mais" (+50); minors deferred: div inside button rows; modal flicker before the puzzle loads.
- Task 12: complete (commits 9ececaf..a35a071, review clean). Ruling: spec §7 "Testar" button for the Stockfish path is satisfied by Save → status invalidation (no test endpoint exists); spec wording to be aligned in the follow-ups doc — cost if wrong: none. minor (deferred): numeric inputs snap to 0 when cleared; error messages use raw keys.
- Controller verification: desktop training flow on the served build OK (move accepted, review recorded, line shown, no console errors); mobile 375px: Painel/Treinar/Erros OK; Partidas had horizontal overflow (422px) and the sticky bottom bar fell below the viewport. Ruling: fixed bottom bar on mobile + minmax(0,1fr) tracks + overflow-x hidden (CSS-only fix dispatched) — cost if wrong: none.
- Final review: With fixes. Ruling: ONE fix wave — Important 1 (job-finished watcher invalidating dashboard/queue/games/game/mistakes/leeches/status on running→idle|error, + review submit invalidation), Important 2 (StrictMode-safe session start: promise in ref), Important 3 (scrollable modal); cheap minors folded: assets/ excluded from SPA fallback (+test), date column 84px on mobile, `classical` in category selects, nav(-1) fallback to /treinar, local_url honours CHESS_TRAINER_HOST loopback note; deferred: Cache-Control on index.html, transport.kill vs close(), server-side ranges for *_seconds, finish elapsed 1 s short, Board.set deps, Modal Esc/focus trap, safe-area inset, "Análise em andamento" for any job, stockfish path masking, §3.5 position param, §6 modal vs side panel — cost if wrong: none material.

## Pendências deferidas (triadas pela revisão final)
- Task 3: minor (deferred): shallow header merge in request(); unused path param in post() helper.
- Task 5: minor (deferred): Board set() on every render (chessground diffs internally; check drag during async fen change in manual verification); fresh Map per render when dests omitted.
- Task 6: minor (deferred): unobservable "solved" phase; re-entrancy guards on render state; render-phase ref writes; useRef eager args; consecutive engine plies deadlock (backend never produces); setState after unmount during submit.
- Task 7: minor (deferred): `now` in effect deps unstable for inline lambdas; plannedMinutes <= 0 expires immediately (untested).
- Task 8: minor (deferred): qc.invalidateQueries() unscoped in finish; LineViewer keydown per instance (use keyboard=false in MistakeDetail, Task 11).
- Task 8: minor (deferred): Continuar path bypasses the advancing guard (edge: batch exhausted exactly at expiry).
- Task 10: minor (deferred): GameDetailPage keeps `current` across param-only navigation (no route key).
- Task 11: review Approved; Important (plan-inherited): 200 chessground instances per page. Ruling: fix round — limit 50 with "Mostrar mais" (+50); minors deferred: div inside button rows; modal flicker before the puzzle loads.
- Backend time-cap: fix round 1 re-review clean (commits 88c4110, d2915d0). minor (deferred): double restart on timeout; TimeoutError ⊂ OSError redundancy.
- Task 12: complete (commits 9ececaf..a35a071, review clean). Ruling: spec §7 "Testar" button for the Stockfish path is satisfied by Save → status invalidation (no test endpoint exists); spec wording to be aligned in the follow-ups doc — cost if wrong: none. minor (deferred): numeric inputs snap to 0 when cleared; error messages use raw keys.
- Final review: With fixes. Ruling: ONE fix wave — Important 1 (job-finished watcher invalidating dashboard/queue/games/game/mistakes/leeches/status on running→idle|error, + review submit invalidation), Important 2 (StrictMode-safe session start: promise in ref), Important 3 (scrollable modal); cheap minors folded: assets/ excluded from SPA fallback (+test), date column 84px on mobile, `classical` in category selects, nav(-1) fallback to /treinar, local_url honours CHESS_TRAINER_HOST loopback note; deferred: Cache-Control on index.html, transport.kill vs close(), server-side ranges for *_seconds, finish elapsed 1 s short, Board.set deps, Modal Esc/focus trap, safe-area inset, "Análise em andamento" for any job, stockfish path masking, §3.5 position param, §6 modal vs side panel — cost if wrong: none material.

## Observações da revisão final não corrigidas
- content padding-bottom no celular não soma env(safe-area-inset-bottom) (últimos ~10 px sob a barra em iPhones com indicador).
- Cache-Control para index.html; transport.kill() fora do event loop (preferir SimpleEngine.close()); faixas de *_seconds só no cliente; tempo do resumo até 1 s menor; Board.set() a cada render; Modal sem Esc/foco; "Análise em andamento" para qualquer job; caminho errado do Stockfish mascarado pelo fallback; ?position= no resumo não é lido; §6 detalhe é modal, não painel lateral, e mostra só a refutação.

## Verificação
- Controller verification: desktop training flow on the served build OK (move accepted, review recorded, line shown, no console errors); mobile 375px: Painel/Treinar/Erros OK; Partidas had horizontal overflow (422px) and the sticky bottom bar fell below the viewport. Ruling: fixed bottom bar on mobile + minmax(0,1fr) tracks + overflow-x hidden (CSS-only fix dispatched) — cost if wrong: none.
- Mobile fix a0109b6 verified: scrollWidth 375, .nav fixed and visible. Cosmetic leftover: date column ellipsized on mobile (72px); widen to 84px in the final fix wave.
