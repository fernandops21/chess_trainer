# Evitar completo e análise — pendências e decisões (2026-09-05)

## Decisões
- Ruling: in-place branch (as before) — cost if wrong: none.
- | T4 plan snippet SAN loop | plan's inline snippet is wrong (push before san); plan says how to write it correctly | Ruling: implementer follows the corrected instruction |
- | T3 regenerate_avoid test | plan admits the scholar's-mate scenario may yield no avoid; assert preservation + n == count | Ruling: acceptable |
- Task 4: review Approved with 2 Importants (no shutdown cleanup of the interactive engine; dead engine kept after a plain EngineError) + dead except order. Ruling: fix round after Task 5 lands (lifespan/shutdown close, reset _engine on EngineError + test, remove dead except) — cost if wrong: none.
- Task 3: review Approved with 3 Importants. Ruling: fix round (batched with Task 4's) — mate mode makes the gap infinite regardless of the second line (alternate mates handled by _is_close/_final_alternatives like punish); replace the duplicate "without materialization" test with a two-line positional fake; add a happy-path test creating an avoid via regenerate_avoid; fix spec §3 wording — cost if wrong: none. minor (deferred): reclassification side effect on kind=avoid; cancel mid-run loses avoid puzzles; avoid_gap==alt_window boundary; draft_avoids duplication; engine cost doubled for "me" mistakes.
- Task 5: review Needs fixes (1 Important: playLine timer not cleared on unmount). Ruling: fold the fix (unmount cleanup effect + ⏮ enabled when fens.length > 1) into the Task 6 dispatch; Task 6's review verifies it — cost if wrong: none.
- Final review: With fixes. Ruling: ONE fix wave — Important 1 (Explorar from the session result opens in a new tab: target=_blank rel=noopener), Important 2 (InteractiveAnalyzer: any exception drops the engine; never cache non-terminal empty lines → raise RuntimeError → 503; board.is_valid() → 400); minors folded: undo/goTo clear the playLine timer; key={fen} on AnalysisBoard; interactive engine threads=2 hash=64; Settings button "Regerar só os evitar" (kind=avoid) with confirmation copy stating avoid reviews are lost; README wording. Deferred: playLine per-move fetch lag, epd cache key, draft_avoids duplication, aria-current, reclassify flag, cancel-mid-run note in dialog — cost if wrong: none.

## Pendências deferidas
- Task 1: minor (deferred): N+1 lazy load of position.puzzles in queue/leeches lists.
- Task 2: minor (deferred): bestSan fallback ineffective for empty solutions (never produced); eager sibling fetch.
- Task 3: review Approved with 3 Importants. Ruling: fix round (batched with Task 4's) — mate mode makes the gap infinite regardless of the second line (alternate mates handled by _is_close/_final_alternatives like punish); replace the duplicate "without materialization" test with a two-line positional fake; add a happy-path test creating an avoid via regenerate_avoid; fix spec §3 wording — cost if wrong: none. minor (deferred): reclassification side effect on kind=avoid; cancel mid-run loses avoid puzzles; avoid_gap==alt_window boundary; draft_avoids duplication; engine cost doubled for "me" mistakes.
- Backend fix round (T3+T4): re-review clean (commit 590ec63; 7/7 addressed). Task 3: complete. Task 4: complete. minor (deferred): double restart when StockfishEngine self-heals then InteractiveAnalyzer closes it; JobRunner engine not closed on shutdown (jobs close their own engine in finally).
- Task 5: complete (fix folded into Task 6, verified). Task 6: complete (commit 13fde30, review clean). minor (deferred): README says "Explorar" for the game page button labelled "Explorar daqui".
- Final review: With fixes. Ruling: ONE fix wave — Important 1 (Explorar from the session result opens in a new tab: target=_blank rel=noopener), Important 2 (InteractiveAnalyzer: any exception drops the engine; never cache non-terminal empty lines → raise RuntimeError → 503; board.is_valid() → 400); minors folded: undo/goTo clear the playLine timer; key={fen} on AnalysisBoard; interactive engine threads=2 hash=64; Settings button "Regerar só os evitar" (kind=avoid) with confirmation copy stating avoid reviews are lost; README wording. Deferred: playLine per-move fetch lag, epd cache key, draft_avoids duplication, aria-current, reclassify flag, cancel-mid-run note in dialog — cost if wrong: none.

## Da revisão final (não corrigidas)
- playLine dispara uma análise por lance intermediário (atraso até ~30 s em linhas longas); chave do cache por FEN com contadores (usar EPD); draft_avoids duplica draft_puzzles; aria-current na lista de lances; flag reclassify em _regenerate; aviso no diálogo de que cancelar a regeração perde os evitar; dica em Configurações de que avoid_gap_cp também limita o alvo de material; teste de componente do ResultPanel para o evitar.
