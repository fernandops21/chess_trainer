# Tema escuro, página de progresso e pequenos — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** (1) Tema escuro alternável com o claro, guardado no navegador. (2) Página "Progresso" com rating de táticas ao longo do tempo, revisões por dia, acerto por tema e por fonte e sequência de dias. (3) Pequenos: "ignorar o limite hoje" nos novos; reimportar o PGN exportado daqui atualiza o estudo em vez de criar outro.

**Aprovado em conversa (2026-09-08, "pode mandar brasa nos outros updates").**

## Global Constraints
- Português na UI, comentários e README; nunca "regerar/regeração". Suítes verdes (backend 486, frontend 603). Testes frontend só em `frontend/tests/**` (Vitest + Testing Library, sem jest-dom). Commits com o trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` via `git commit -F` (arquivo UTF-8 em `.superpowers/`); `git add` com caminhos explícitos. Nunca tocar em `backend/data/*`. Nada apaga dados. `new Chess(` nunca: só `novoChess` de `frontend/src/lib/chess.ts`.
- Tarefas 1 e 2 rodam em paralelo: a Task 1 é dona de `Nav.tsx`, `tokens.css`, `base.css`; a Task 2 não toca neles (o link para a página nova entra em `App.tsx` e no `DashboardPage.tsx`). Nenhuma delas toca em `SessionStart.tsx`, no parser de estudos nem em `service.py` (Task 3).

## Tasks

### Task 1 — frontend: tema escuro
Files: `frontend/src/styles/tokens.css` (paleta escura em `:root[data-theme="dark"]`), `frontend/src/styles/base.css` (trocar cores fixas por tokens onde ainda há `#fff`, `#3d3a36`, `rgba(...)` etc. — exceto a barra de avaliação, que é preta e branca de propósito, e os ícones de classificação), `frontend/src/lib/theme.ts` (novo: `getTema(): "claro" | "escuro"`, `setTema`, `useTema()` — guardado em `storage` chave `tema`; sem valor guardado segue `prefers-color-scheme`; aplica `document.documentElement.dataset.theme = "dark" | ""`), `frontend/src/main.tsx` (aplica o tema antes do primeiro render, para não piscar), `frontend/src/components/Nav.tsx` (botão "Tema" ao lado do "Som", ícone 🌙/☀️, `aria-pressed`, `aria-label "Tema escuro"/"Tema claro"`), README (seção curta "Tema").
Paleta escura (ponto de partida; ajuste para contraste ≥ 4.5:1 no texto): `--bg #1b1917`, `--surface #262421`, `--ink #ece7de`, `--muted #a89f94`, `--line #3b3632`, `--brand #e6dccb`, `--brand-ink #1b1917`, `--ok #7cc47f`, `--bad #ff7b72`, `--warn #f5c451`, `--accent #5a4a14` (destaque do lance atual: deve continuar legível com `--ink`), `--shadow 0 1px 3px rgba(0,0,0,.5)`. O tabuleiro (chessground marrom) e as peças não mudam. Conferir no `base.css`: `.msg.ok/.bad` (fundos claros fixos → versões escuras via tokens ou `color-mix`), `.tag`, `.badge`, `.modal-backdrop`, `.node-menu`, `.previa`, `.livro-pagina`, `.movelist`, `.mistakerow`, inputs/selects/textarea (fundo `--surface`, texto `--ink`, borda `--line`), botões `.primary`/`.danger`, `.nav` (já é escuro: no tema escuro fica um tom acima do fundo), MiniBoard, cartões do painel. Nada de `color` fixo fora dos ícones SVG e da barra.
Tests: `frontend/tests/theme.test.ts` (padrão segue `prefers-color-scheme` via `matchMedia` dublê; `setTema("escuro")` grava e aplica `data-theme="dark"`; `"claro"` limpa), `frontend/tests/nav.test.tsx` (botão alterna o atributo e o rótulo). Rodar `npx vitest run`, `npx tsc --noEmit -p .`, `npm run build`. Commit `feat(ui): tema escuro alternável, guardado no navegador`.

### Task 2 — página "Progresso" (backend + frontend)
Backend: `GET /api/stats/progress?days=90` (`api/routes/tactics.py` ou novo `api/routes/stats.py` registrado em `api/app.py` — seguir onde está `/stats/themes`), lógica em `core/stats.py`: `progress(db, since, now) -> dict` com `reviews_per_day: [{day: "YYYY-MM-DD", correct: int, wrong: int}]` (dias sem revisão não entram), `tactics_rating: [{at: iso, rating: int}]` (um ponto por tentativa, `rating_after`, ordem cronológica), `by_source: {own|lichess|study: {reviews: int, correct: int}}`, `streak_days` (mesmo cálculo do painel — reaproveitar), `totals: {reviews, correct, puzzles_in_queue}`. Só leitura. Schema `ProgressOut` em `api/schemas.py`. Testes em `backend/tests/test_api_stats.py` (fixture com revisões em 3 dias e 2 fontes, 3 tentativas de tática: contagens e ordem).
Frontend: rota `/progresso` → `frontend/src/pages/ProgressPage.tsx` (`<h1>Progresso</h1>`), `api.progress(days)` + `useProgress(days)`; seletor de período (30/90/365 dias); gráficos em SVG inline sem biblioteca, em `frontend/src/components/charts/` (`LineChart.tsx` para o rating, `BarChart.tsx` empilhado certo/errado por dia), cores dos tokens (`--ok`, `--bad`, `--brand`, `--muted`) para funcionarem nos dois temas, eixo com 4–5 marcas, `role="img"` + `aria-label` resumindo; tabela "por tema" (reaproveitar `useThemeStats`) e "por fonte" com acerto em %; cartão da sequência de dias. Estado vazio: "Ainda não há revisões neste período." Link "Progresso" no `DashboardPage` (ao lado dos cartões) e rota em `App.tsx`; **não** mexer em `Nav.tsx` (Task 1 é dona) — deixar anotado no relatório que o item de menu entra depois.
Tests: `frontend/tests/progressPage.test.tsx` (API dublada: rótulos, tabela por fonte com %, estado vazio, troca de período refaz a consulta), `frontend/tests/charts.test.tsx` (LineChart/BarChart com 3 pontos: número de elementos e `aria-label`). README seção "Progresso". Commit `feat(progresso): página com rating de táticas, revisões por dia, acerto por tema e por fonte`.

### Task 3 — pequenos (backend + frontend)
1. **Ignorar o limite hoje**: `QueueFilters.ignore_limit: bool = False` e `GET /api/queue?ignore_limit=1` → no modo `new`, `remaining = new_available` (sem tocar em `count_new_reviewed_today`); `SessionStart.tsx` ganha, no modo "Novos", um checkbox "ignorar o limite diário hoje" (não persistido), mandado nos `filters`; o texto de ajuda muda para "…até o limite diário (ou sem limite, se marcado)". Testes backend (`test_api_training.py`) e frontend (`sessionStart.test.tsx`: o parâmetro vai na URL).
2. **Reimportar PGN exportado daqui**: `study_pgn`/`chapter_pgn` (`core/studies/tree.py`) escrevem o cabeçalho `[ChessTrainerStudy "<study.id>"]` em cada capítulo; o parser (`core/studies/parser.py`) lê para `ParsedStudy.local_id`; `_find_study` (`service.py`) procura primeiro por esse id, depois `lichess_id`, depois `source_url`. Assim colar de volta o PGN exportado atualiza o estudo (capítulos casam pelo `ChapterURL` quando há, senão pelo nome — regra já existente). Testes: exportar um estudo local, reimportar o texto → mesmo `Study.id`, mesma contagem de capítulos, exercícios preservados.
3. README: "Modos de treino" (checkbox) e "Criar estudos aqui" (ida e volta pelo PGN).
Rodar as duas suítes. Commit `feat: ignorar o limite diário de novos; reimportar o PGN exportado atualiza o estudo`.
