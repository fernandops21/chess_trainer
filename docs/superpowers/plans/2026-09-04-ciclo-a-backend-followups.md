# Ciclo A backend — pendências e decisões (extraído do ledger de execução em 2026-09-04)

## Decisões tomadas durante a execução
- Ruling: work in-place on feat/ciclo-a-backend instead of a worktree — repo is docs-only, nothing to protect, user views files here — cost if wrong: none material.
- | T16 .gitignore | plan says Create; root .gitignore now exists (created pre-flight for .superpowers) | Ruling: T16 modifies instead of creates — cost if wrong: none |
- | T6 self-consistency | test_find_stockfish_prefers_configured has an assert ending in `or True` (asserts nothing) | Ruling: implementer drops that second assert line; keep only the configured-path assert — cost if wrong: slightly less coverage of find_stockfish fallback |
- | T15 self-consistency | `ready(tmp_path)` fixture takes unused tmp_path | Ruling: implementer may drop the unused param — cosmetic |
- Task 2: Ruling: reviewer flagged format_score not clamping non-mate scores (plan-mandated) — code stands: format_score is display-only and should show true magnitude; clamp is for the material-target conversion (to_pawns) per spec §7.1 — cost if wrong: cosmetic oversized numbers in the UI.
- Task 5: Ruling: spec §4 "pula variantes com aviso" has no log line — ImportResult.filtered is the visible signal for now; a logger comes with the API job messages if needed — cost if wrong: variant skips are silent in the log.
- Task 7: Ruling: silent eval=0 fallback on empty engine output (plan-mandated) is a real defect — change _evaluate to raise chess.engine.EngineError on empty lines for a non-terminal position; pipeline (Task 11) already catches EngineError and restarts — cost if wrong: a game with a genuinely unanalysable position is retried instead of stored with 0.
- Task 9: Ruling: implementer removed `if after.is_game_over(): return None` to make test_hanging_queen pass; the plan's test FEN was wrong (K+N vs K after the capture = insufficient material = draw, so discarding is correct). Restore the check; change HANGING_QUEEN to "4k3/8/8/3q4/8/2N5/7P/4K3 w - - 0 1" (white pawn h2, verified with python-chess); keep implementer's e1e2 as the quiet alternative (e1d1 was illegal in the plan) — cost if wrong: puzzles whose winning capture leaves a technically drawn position are discarded, which is the desired behavior.
- Task 9: Ruling: reviewer's 4 Important findings (plan-mandated) are accepted as real: alternatives must also be rejected when they end the game in a draw; a short/inconsistent alt PV means "not materialized"; the terminal material path must check that the reply did not end the game in a draw; add regression test for the restored is_game_over guard — cost if wrong: slightly fewer puzzles accepted (conservative).
- Task 9: Ruling: finding 3 must use plain after_reply.is_game_over(); the RECAPTURE test FEN was bare kings after Kxd5 (a draw) — change it to "8/8/4k3/3q4/8/8/7P/3RK3 w - - 0 1" (white pawn h2, verified) — cost if wrong: none; consistent with the earlier dead-draw ruling.
- Task 10: Ruling: two plan-mandated Importants (fork value uses pre-promotion piece; no test proving theme priority order) deferred — theme is a filter label, not a correctness input; promotion forks are rare — cost if wrong: some mislabeled themes.
- Task 12: Ruling: interval rounding uses half-up (int(x+0.5)) instead of Python's banker's round, because the plan's expected 33 for 32.5 implies half-up — cost if wrong: none (intervals differ by at most 1 day).
- Task 13: Ruling: DST bug in local_day_start (plan-mandated) is real; fix by re-resolving the offset at local midnight (naive local → astimezone(utc)) and strengthen the test to assert local 00:00 on the same local date — cost if wrong: none (Brazil has no DST; fix is one line).
- Task 14: Ruling: three plan-mandated Importants accepted — close ChessComClient in the import job's finally; engine_probe must close the engine instance it creates (probe stays factory-based so tests keep working); add a 409-busy test using a blocking job — cost if wrong: none.
- Final review: With fixes. Ruling: fix wave includes Critical #1 (engine work outside write transactions + WAL + timeout 30 + file-DB concurrency test), Important #2 (target = floor_to_piece(min(clamp(drop), solver_eval)/100) + spec §7.1 note), #3 (cancellable jobs: should_stop between games + POST /api/jobs/cancel), #4 (regenerate_all EngineError handling), #5 (404 on unknown session_id), minors: unused import, avoid puzzle skipped when best move == move played — cost if wrong: extra surface in this branch; all are small and covered by tests.
- Ruling: per-game cost (≈3 min for short games, >45 min for a 139-ply game) is load-bearing for the real 1675-game import → follow-up before the frontend: PV pre-check before the engine loop, lower depth for the defender reply, consider puzzle_depth 18–20, cancel granularity per position — cost if wrong: none; already in the reviewer's recommendations.

## Pendências deferidas (triadas pela revisão final)
- Task 2: minor (deferred): no test at exact MATE_THRESHOLD boundary (90000).
- Task 3: minor (deferred): Position.puzzles / TrainingSession.reviews without cascade (no independent Position delete path today); FK columns without explicit String(36); save_settings mutates its argument.
- Task 4: minor (deferred): non-JSON 2xx body raises JSONDecodeError instead of ChessComError.
- Task 5: minor (deferred): per-game dedup SELECT (N+1); silent .get defaults in parse_game; no rollback/comment for mid-month parse exceptions.
- Task 6: minor (deferred): dead isinstance(dict) branch and redundant int() in StockfishEngine.analyse; is_game_over guard broader than "no legal moves"; close() does not swallow OSError.
- Task 9: minor (deferred): mate scores inside the 50cp window when material mode latched; dead `target <= 0` branch untested; wasted reply analysis before intermediate-ambiguity discard; misleading comment/unused default in test_gain_must_survive_best_reply; puzzle_config_from untested.
- Task 9: minor (deferred): no dedicated tests for alt.pv[0] != alt.move and illegal alt.pv[1] branches; real engine returns [] on insufficient-material positions (same discard path as stalemate, acceptable).
- Task 10: minor (deferred): promotion-aware moved_value in infer_theme; overlapping-condition priority test; castling not special-cased in _attack_pairs; no boundary test at gap == avoid_gap_cp.
- Task 11: minor (deferred): EngineError during puzzle generation untested; tied played_at in limit test; regenerate_all has no EngineError handling.
- Task 12: minor (deferred): sub-day truncation of elapsed days; half-up helper not sign-safe (unreachable).
- Task 13: minor (deferred): new_available count query carries ORDER BY; make_puzzle ignores category/played_at when game is passed.
- Task 13: minor (deferred): the day-start test date is not a DST-transition date, so it would pass under the old code too.
- Task 14: minor (deferred): JobRunner.progress/snapshot without lock (benign); settings loaded twice in _engine_job (TOCTOU, single-job model).
- Task 15: minor (deferred): unused import count_new_reviewed_today in training.py; N+1 lazy loads in _puzzle_out; _streak_days loads all review timestamps; `by` param unvalidated on /api/mistakes.
- Final review: deferred to follow-up: perf (PV pre-check before engine loop; lower depth for reply), config.py placement, EPD-based dedup, mover_color from FEN, limit bounds/selectinload, X_OK check in find_stockfish, time_class missing → skip, logging of variant skips, Task 5/9/10 items listed by the reviewer.
- Final review fix wave: re-review clean (commits 0c66ca3..4ff644d, 6/6 addressed). Residual minors (deferred): cancel after fn returned stamps "cancelado" on a completed job; cancelled import still writes last_import_at; regenerate_all deletes everything up front so a cancel leaves a partial puzzle set; concurrency test is timing-based (elapsed < 1.0 vs 1.5 s sleep); no direct should_stop tests for regenerate_all/import_games.

## Verificação manual
- Task 16: complete — Stockfish 17.x installed in backend/engines (user-approved download), slow test passed, 3 real games analyzed (2 daily vs Coach-Mittens, 1 rapid) → 14 mistakes, 5 puzzles, all solutions legal and ending in concrete material gain; queue/dashboard/cancel verified live. Game 4 (139 plies) exceeded 45 min of puzzle generation; run aborted by killing the server (DB consistent by design).
