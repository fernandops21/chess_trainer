# Lances clicáveis no texto e defesa mais resistente na geração — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** (1) A resposta do adversário nos puzzles dos próprios erros passa a vir da mesma busca funda do lance do solver, escolhendo a defesa mais resistente. (2) Lances escritos em texto (comentário de capítulo, enunciado, mensagem da refutação, "Certo! — comentário") viram links que mostram a posição no tabuleiro como prévia, sem marcação especial do autor.

**Spec (aprovado em conversa, 2026-09-08: "fecha os dois, pode fazer"):** ver as regras de cada tarefa.

## Global Constraints
- Português na UI e nos comentários; nunca "regerar/regeração" (a UI diz "recriar"). Suítes verdes (backend 463, frontend 541). Testes frontend só em `frontend/tests/**` (Vitest + Testing Library, sem jest-dom). Commits com o trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` via `git commit -F` (arquivo UTF-8 em `.superpowers/`); `git add` com caminhos explícitos. Nunca tocar em `backend/data/*`. Nenhuma tarefa apaga dados nem roda a recriação: isso é decisão do usuário, depois.

## Tasks

### Task 1 — backend: resposta do adversário com a busca funda
Hoje (`backend/chess_trainer/core/puzzles/generator.py` ~176-186): o lance do solver sai de `engine.analyse(current, cfg.depth, multipv=3, max_seconds=cfg.search_seconds)` (profundidade 22, 20 s); a resposta do adversário sai de `engine.analyse(after, cfg.reply_depth, multipv=1, max_seconds=cfg.reply_seconds)` (profundidade 16, 10 s), exceto em modo mate. Na prática as duas buscas na profundidade 16 (a do puzzle e a da Análise) discordam entre defesas quase iguais, e o puzzle às vezes mostra a defesa mais fraca.
Regra nova: a resposta do adversário usa **sempre** `cfg.depth` e `cfg.search_seconds`, com `multipv=1` (a primeira linha da busca do lado a jogar já é a defesa mais resistente); o caso `mate_mode` fica igual ao geral. `PuzzleConfig` perde `reply_depth` e `reply_seconds`; `config.py` (`puzzle_config_from_settings` ou equivalente, ~85-88) deixa de calculá-los; `AppSettings.puzzle_reply_seconds`, `SettingsOut.puzzle_reply_seconds` e `SettingsIn.puzzle_reply_seconds` somem (um `PUT` com a chave antiga é ignorado, como qualquer chave desconhecida do pydantic — conferir `extra` no modelo; se for `forbid`, aceitar e ignorar); o comentário ~230 sobre "resposta rasa" é atualizado. README: a linha sobre "tempo máximo da resposta do defensor" sai e a seção dos puzzles diz que a resposta do adversário é a defesa mais resistente na mesma profundidade do lance do solver.
Files: `generator.py`, `config.py`, `api/schemas.py`, `README.md`; tests `backend/tests/test_generator.py` (ou o arquivo que testa `_materializing_line`/`generate_punish` com engine dublê: afirmar que a chamada da resposta usa `depth == cfg.depth` e `max_seconds == cfg.search_seconds`), `backend/tests/test_config.py`, `backend/tests/test_api_system.py` (o campo não volta mais no GET; PUT com a chave antiga não dá 422).
Frontend (na mesma tarefa, pequeno): `frontend/src/api/types.ts` (`Settings.puzzle_reply_seconds` sai), `frontend/src/pages/SettingsPage.tsx` (campo "tempo máximo da resposta do defensor (s)" sai), `frontend/tests/fixtures/settings.ts` e `frontend/tests/analysisBoard.test.tsx` ~71 (campo sai), `frontend/tests/settingsPage.test.tsx` se o referenciar. Rodar `uv run pytest -q` em `backend/` e `npx vitest run` + `npx tsc --noEmit -p .` + `npm run build` em `frontend/`.
Commit `feat(puzzles): resposta do adversário com a mesma busca funda do lance do solver`.

### Task 2 — frontend: lances clicáveis no texto com prévia no tabuleiro
Regras:
- **Reconhecimento** (`frontend/src/analysis/moveText.ts`, novo): `export function segmentar(texto: string, fenAncora: string): Segmento[]`, `Segmento = { kind: "texto"; text: string } | { kind: "lance"; text: string; san: string; uci: string; fen: string; lastMove: [Key, Key]; linha: LanceDaLinha[] }` onde `LanceDaLinha = { san, uci, fen, lastMove }` é a sequência da âncora até este lance inclusive. Token candidato: regex sobre o texto, com prefixo opcional de número (`12.`, `12...`, `12. `) que fica dentro do `text` do segmento, SAN (`O-O`, `O-O-O`, `[KQRBN][a-h]?[1-8]?x?[a-h][1-8]`, `[a-h]x?[a-h]?[1-8](=[QRBN])?`), sufixo `[+#]?` e `[!?]{0,2}`; o token precisa estar entre limites de palavra (não pode ser parte de "Nc3-" dentro de outra palavra alfanumérica). Cada token é testado com `novoChess(fenAtual).move(sanSemSufixos)`: legal → vira segmento "lance" e a posição atual avança; ilegal → tenta de novo a partir da âncora (recomeço de sequência); ainda ilegal → texto comum. Tokens que não parecem lance nunca reiniciam a sequência (assim "d5? Nb4 — avaliação cai de +2.56 para +1.62 · segue Nc3 Qb6" encadeia d5, Nb4, Nc3, Qb6 a partir da âncora). FEN inválido → tudo texto.
- **Componente** `frontend/src/analysis/TextoComLances.tsx`: `<TextoComLances texto fen onPrevia={(linha: LanceDaLinha[]) => void} />` renderiza os segmentos; os lances como `<button type="button" className="lance-no-texto" title="mostrar no tabuleiro">` com o `text` original (mantendo número e sufixos); `white-space: pre-wrap` preservado. CSS em `base.css`: botão sem borda/fundo, mesma fonte e cor do texto, sublinhado pontilhado, cursor pointer, `:hover` cor de link.
- **Prévia no tabuleiro de análise** (`frontend/src/analysis/AnalysisBoard.tsx`): estado `previa: LanceDaLinha[] | null`. Com prévia, o `Board` recebe `fen`/`lastMove` do último lance da prévia e não aceita lances (`movableColor` indefinido); acima do tabuleiro uma faixa `.previa` com "prévia: Nc3 Qb6 · " + botão "voltar" e, se `editable`, botão "adicionar como variação" que chama `mt.insertLine(ucis da prévia)` (mesma função das linhas da engine) e limpa a prévia. Qualquer navegação (`goTo`, setas do teclado, botões ⏮ ◀ ▶, clicar num lance da lista, jogar no tabuleiro) limpa a prévia. O comentário do lance e o enunciado (leitura, ~274-277) passam a usar `TextoComLances` com âncora `mt.fen` (posição do nó; no enunciado, `mt.tree.fen`). No editor a textarea continua textarea (a prévia é só na leitura do comentário, que no editor aparece abaixo da textarea? Não: no editor não há visualização do comentário além da textarea; então no editor os lances clicáveis não aparecem — só na leitura). Engine ligada com prévia: a análise mostrada é a da posição da prévia (`useAnalyse(previa ? ultimaFen : mt.fen)`), sem badge de classificação nem livro durante a prévia.
- **Prévia no exercício** (`frontend/src/train/usePuzzle.ts` + `PuzzleView.tsx`): `message` ganha `fen?: string` (âncora): na refutação, `fenAntes` (posição antes do lance errado); no "Certo! — comentário" (`okMessage`), a posição **antes** do lance do solver recém-jogado (o comentário do autor fala do lance que acabou de ser jogado e do que vem depois: âncora = posição antes dele, e o próprio lance vira o primeiro link). `PuzzleView` renderiza `state.message.text` com `TextoComLances` quando `message.fen` existe; estado local `previa` com `fen`/`lastMove`; enquanto houver prévia, o `Board` mostra a prévia e não aceita lances, e aparece a faixa "prévia: … · voltar"; a prévia é limpa quando `state.fen` muda, ao clicar "voltar", "Tentar de novo" ou "Mostrar peça". Nenhuma chamada extra à engine no exercício.
- **Navegar o histórico do exercício** (pedido em 2026-09-08: "os puzzles estão iniciando com lance do adversário mas sem a possibilidade de usar setinha pra voltar e ver qual foi de fato o lance"): `PuzzleView` ganha botões `⏮ ◀ ▶ ⏭` (aria-labels "Início", "Lance anterior", "Próximo lance", "Posição atual") abaixo do tabuleiro, como no tabuleiro de análise, e as setas ←/→ do teclado (Home/End para os extremos) quando o foco não está num campo de texto. O histórico é a lista de posições do exercício: `fen_before` (com `lastMove` nulo) quando existe, depois `fen_start` (com o `last_move` do adversário destacado), depois a posição após cada lance já jogado na sessão (solver e engine), até a posição viva. Voltar mostra a posição escolhida como prévia (mesmo estado `previa` dos lances clicáveis: tabuleiro sem `movableColor`, faixa "posição 2 de 5 · voltar ao lance atual"); ▶/⏭ até a viva limpa a prévia. Os botões ficam desabilitados nos extremos; durante `intro`, `engine_replying`, `refuting` e `refuted` os botões existem mas a navegação só cobre o que já está no histórico (na refutação o histórico inclui o lance errado e a réplica como últimas posições, para o usuário poder voltar e comparar). `usePuzzle` expõe `history: { fen: string; lastMove?: [Key, Key] }[]` (derivado do `chessRef` + intro + refutação) para a view não recalcular nada. Testes em `puzzleView.test.tsx`: com `fen_before`, ◀ na posição inicial mostra `fen_before` no `Board` dublê sem `lastMove` e sem `movableColor`; ▶ volta à viva com `movableColor`; após um lance certo com resposta da engine, ⏮ vai ao começo e ⏭ à viva; setas do teclado fazem o mesmo; durante a refutação ◀ mostra a posição antes do lance errado.
- **Painel de resultado** (`ResultPanel`/`TacticResultPanel` usam `AnalysisBoard`): ganha a prévia de graça pelos comentários da solução, se existirem; nada a fazer além de conferir que não quebra.
Files: `moveText.ts`, `TextoComLances.tsx` (novos), `AnalysisBoard.tsx`, `usePuzzle.ts`, `PuzzleView.tsx`, `base.css`, README (seção "Criar estudos aqui": "Lances escritos no comentário viram links: clique para ver a posição; no editor, 'adicionar como variação'"); tests `frontend/tests/moveText.test.ts` (sequência, recomeço após lance ilegal, número de lance colado, sufixos `+ # ! ?`, roque, palavra que parece lance mas é ilegal fica texto, FEN inválido), `textoComLances.test.tsx` (botões e `onPrevia` com a linha até o clique), `analysisBoard.test.tsx` (comentário com lances → clique mostra prévia no `Board` dublê, faixa, "voltar" restaura, navegação limpa, "adicionar como variação" no editor insere na árvore), `puzzleView.test.tsx` (mensagem da refutação com lances clicáveis; clique põe a prévia no `Board` e o tabuleiro fica sem `movableColor`; "Tentar de novo" limpa), `usePuzzle.test.ts` (`message.fen` na refutação e no ok com comentário).
Rodar `npx vitest run`, `npx tsc --noEmit -p .`, `npm run build`. Commit `feat(analise): lances escritos no texto viram links com prévia no tabuleiro`.

### Task 3 — frontend: modo livro na leitura do capítulo
Pedido (2026-09-08, "fecha o modo livro"): a lista de lances com comentários cortados em uma linha não serve para ler um livro (o usuário pôs os comentários do *Logical Chess* no capítulo Scheve × Teichmann). Na leitura do capítulo, a coluna da direita vira texto corrido, como uma página de livro.
Regras:
- **Componente** `frontend/src/studies/BookView.tsx`: `<BookView tree currentId onGoTo classes? bookIds? />`. Percorre a árvore a partir da raiz e produz parágrafos: (a) o enunciado (`tree.intro`) como primeiro parágrafo, via `TextoComLances` com âncora `tree.fen`; (b) lances **sem** comentário consecutivos formam uma "corrida" renderizada num só parágrafo compacto `<p className="livro-lances">` ("5. O-O d6 6. d4 Bb6"); (c) um lance **com** comentário fecha a corrida em andamento e vira um parágrafo `<p className="livro-par">` com o lance em negrito (com número: "3. Bc4" nas brancas, "3...Bc5" nas pretas quando o lance abre o parágrafo) seguido do comentário completo, via `TextoComLances` com âncora = posição **depois** do lance (`fenAt(tree, node.id)`); (d) variações (irmãos além do primeiro filho) entram logo depois do parágrafo do lance principal correspondente como blocos recuados `<div className="livro-variacao">`, recursivos, com as mesmas regras (o primeiro lance da variação sempre traz o número); (e) NAGs colados ao SAN como na lista de hoje; selos de classificação (`classes`) e de livro (`bookIds`) como na lista, quando vierem. Cada lance é um `<button type="button" className="livro-lance">` que chama `onGoTo(id)`; o lance atual leva `className` extra `atual` e, ao mudar, `scrollIntoView({ block: "nearest" })` (guardado com `typeof el.scrollIntoView === "function"` para o jsdom).
- **Tipografia** (`base.css`): `.livro` com fonte de leitura serifada (`Georgia, "Times New Roman", serif`), `font-size: 1.05rem`, `line-height: 1.6`, rolagem própria (`max-height` como o painel atual, `overflow: auto`); `.livro-lance` em negrito, sem sublinhado, mesma cor do texto, `:hover` cor de link, `.atual` com fundo de destaque; `.livro-lances` em cor mais suave; `.livro-variacao` recuado (`margin-left: 1.2em`, borda esquerda fina, fonte 0.95em).
- **Onde entra**: `AnalysisBoard` ganha `layout?: "analise" | "livro"` (padrão `"analise"`, nada muda). Com `"livro"`: a coluna da direita renderiza `BookView` no lugar de `MoveTreeView`, e a caixa de comentário abaixo do tabuleiro **não** é renderizada (o comentário está no texto). O painel da engine continua como está (botão "Analisar com a engine" desligado por padrão; ligado, os selos aparecem no texto). `ChapterViewPage` passa `layout="livro"`; o editor e a Análise continuam com a lista.
- **Tela estreita** (largura ≤ 720px, mesma regra do resto do app — conferir o breakpoint em `base.css`): abaixo do tabuleiro aparece um cartão `.livro-atual` com o comentário completo do lance atual (via `TextoComLances`), escondido em telas largas por CSS; assim no celular, onde a coluna cai para baixo, o comentário da posição fica junto do tabuleiro.
- Prévia dos lances clicáveis (Task 2) vale dentro do texto do livro; clicar no lance em negrito é `onGoTo` (navegação), clicar num lance dentro do comentário é prévia.
Files: `BookView.tsx` (novo), `AnalysisBoard.tsx`, `ChapterViewPage.tsx`, `base.css`, README (seção "Estudos do Lichess" ou "Criar estudos aqui": um parágrafo "Modo livro"); tests `frontend/tests/bookView.test.tsx` (enunciado como primeiro parágrafo; corrida de lances sem comentário num parágrafo só; lance comentado em parágrafo próprio com número certo para brancas e pretas; variação recuada depois do lance principal; lance atual com `atual`; clique chama `onGoTo` com o id; NAG colado), `chapterViewPage.test.tsx` (leitura usa o modo livro: existe `.livro`, não existe `.tree`; comentário do lance aparece inteiro, sem reticências), `analysisBoard.test.tsx` (layout padrão inalterado: `.tree` presente, `.livro` ausente).
Rodar `npx vitest run`, `npx tsc --noEmit -p .`, `npm run build`. Commit `feat(estudos): modo livro na leitura do capítulo`.

### Task 4 — "Na partida você jogou…" nos exercícios de punir (backend + frontend)
Pedido (2026-09-08): no resultado de um exercício "punir", o cartão "Erro do adversário" mostra o lance do adversário (Qb4), que o puzzle já mostra na introdução; o que o usuário quer ver é o lance que **ele** jogou na partida em resposta, para entender por que errou.
Regras:
- **Backend**: `MistakeInfo` (o `mistake` de `PuzzleOut`, `api/schemas.py` ~76-90) ganha `my_reply: MyReplyInfo | None` com `MyReplyInfo { ply: int, move_played: str, move_uci: str, eval_before: int, eval_after: int }`: só para puzzles com `mistake_by == "opponent"` (os "punir"), é a linha de `positions` da mesma partida com `ply == mistake.ply + 1` (a resposta do usuário); `None` quando não existe (fim da partida) ou nos "evitar". Montado em `routes/training.py` onde `MistakeInfo` é criado (~71, `_puzzle_out`), com uma consulta só (sem N+1 na fila: buscar as posições `ply + 1` de todos os puzzles da lista de uma vez, ou aceitar uma consulta por puzzle só em `_puzzle_out` — preferir a consulta única quando `_puzzle_out` for chamado em lote; se for complicado, uma consulta por puzzle é aceitável, documentada). Testes em `backend/tests/test_api_training.py`: punir com resposta → `my_reply` preenchido; punir no último lance da partida → `None`; evitar → `None`.
- **Frontend** (`frontend/src/api/types.ts`, `frontend/src/train/MistakeCard.tsx`, `PuzzleView.tsx` rótulo do botão): no "punir" com `my_reply`, o mini-tabuleiro mostra `fen_start` (a posição do exercício, depois do erro do adversário) com **o lance do usuário** destacado (`my_reply.move_uci`), e o texto vira: "Na partida o adversário jogou **Qb4** (−0.94 → −3.07) blunder. Você respondeu **Nc3** (+3.07 → +0.20) e deixou passar **Nxe5**." — onde o último é o SAN do primeiro lance da solução (calcular com `novoChess(fen_start)` e `move(uci)`); se `my_reply.move_uci` for igual ao primeiro lance da solução (ou a uma alternativa), o texto termina em "Você achou **Nxe5** na partida." Sem `my_reply`, o cartão fica como hoje. Os lances do texto passam por `TextoComLances`? Não: aqui o mini-tabuleiro já mostra; manter texto simples. Rótulo do botão no `PuzzleView` para o punir: "Na partida" (em vez de "Erro do adversário"); no evitar continua "Meu erro (revela o lance que não jogar)". As avaliações seguem o mesmo `formatEval` e o mesmo ponto de vista já usado no cartão (conferir em `Position`: `eval_before`/`eval_after` são do ponto de vista de quem jogou o lance; manter). Testes: `frontend/tests/mistakeCard.test.tsx` (punir com `my_reply`: mini-tabuleiro com `fen_start` e `lastMove` do lance do usuário, texto com os três lances; punir em que o usuário achou a solução; punir sem `my_reply` = comportamento antigo), `puzzleView.test.tsx` (rótulo "Na partida"), `resultPanel.test.tsx` se referenciar o rótulo.
Files: `api/schemas.py`, `api/routes/training.py`, tests backend; `types.ts`, `MistakeCard.tsx`, `PuzzleView.tsx`, tests frontend. Rodar as suítes dos dois lados. Commit `feat(treino): cartão do punir mostra o que você respondeu na partida e o que deixou passar`.

### Task 5 — backend: lado do aluno nos exercícios importados (gamebook com o adversário a jogar)
Bug (2026-09-08, https://lichess.org/study/4JKVAfaE/QtPYhPsi): a posição inicial do capítulo tem o adversário a jogar; o primeiro lance da linha principal é dele (às vezes com o comentário "Jogam as pretas"/"Jogam as brancas"), e o aluno joga a partir do segundo. O parser (`backend/chess_trainer/core/studies/parser.py`, `solution_from_game` ~261-285) toma o lado a jogar na FEN como o aluno (`by = "solver" if i % 2 == 0 else "engine"`), invertendo quem é quem: 13 dos 34 exercícios de estudo do banco do usuário terminam com um lance "engine", sinal da inversão. Exemplos confirmados no estudo 4JKVAfaE: capítulos QtPYhPsi (FEN `w`, linha `34. Re4 {Jogam as pretas} 34... Qh5+ … 38... Bxd4+`, resultado 0-1 → aluno = pretas), jplGgITX (FEN `b`, `24... Bc5 25. Qxh7+ … 27. Rh8#`, 1-0 → brancas), jqdDO7BG, d5GqFvzD (`19... Ne8 {Jogam as brancas} 20. Bxg7+! …`), cn9ywxGv.
Regra nova para o lado do aluno (`solver`), na ordem; a primeira que decide vence:
1. Texto: o enunciado (comentário antes do primeiro lance) ou o comentário do **primeiro** lance casa, sem distinguir maiúsculas/acentos, com `jogam as pretas|pretas jogam|negras jogam|jogam as negras|black to (play|move)` → pretas; `jogam as brancas|brancas jogam|white to (play|move)` → brancas.
2. `[Result]` do capítulo: `1-0` → brancas; `0-1` → pretas (`1/2-1/2` e `*` não decidem).
3. O lado que joga o **último** lance da linha principal (o autor para depois do lance do aluno).
4. O lado a jogar na FEN (comportamento atual).
Consequência: se o aluno **não** é o lado a jogar na FEN, o primeiro lance da linha principal é o lance de introdução do adversário: `fen_before` = FEN do capítulo, `last_move` = esse lance (UCI), `fen_start` = posição depois dele, `side_to_move` = aluno, e a solução começa no segundo lance (índices de `comments`/`wrong_moves`/`shapes` da solução deslocados de acordo — conferir como são indexados hoje). Se o aluno é o lado a jogar, nada muda. A linha precisa continuar com o aluno alternando a partir daí; um lance a mais ou a menos no fim não quebra (a solução pode terminar com o lance do aluno, e um último lance do adversário no fim da linha, se houver, é descartado da solução — hoje ele entra como "engine" final; manter a regra atual para esse caso, só documentar).
Também: a heurística `_parece_exercicio` não muda. `chapter_pgn`/`study_pgn` (exportação) não mudam: a árvore continua guardando a linha inteira; só a solução do puzzle muda.
Files: `parser.py` (+ `service.py` se `fen_before`/`last_move` do puzzle de estudo são montados lá — grep `fen_before` em `core/studies`), tests `backend/tests/test_study_parser.py` (um caso por regra: comentário "Jogam as pretas" com FEN `w`; `[Result "1-0"]` com FEN `b` e primeiro lance das pretas; sem texto nem resultado, último lance decide; nenhum sinal → lado a jogar; e o caso completo QtPYhPsi acima com `fen_before`, `last_move == "b4e4"`, `side_to_move == "black"`, solução começando em `c5h5` com `by == "solver"` e terminando em `g7d4` como `solver`), `backend/tests/test_studies_service.py` (reimportar um estudo cujo exercício muda de lado recria o puzzle — o comportamento de `_recreate_exercise` já existente).
README: seção "Estudos do Lichess", um parágrafo "Quem joga no exercício" com as quatro regras em uma frase cada.
Rodar `uv run pytest -q`. Commit `fix(estudos): aluno do exercício pelo texto, resultado ou último lance; primeiro lance do adversário vira introdução`.
